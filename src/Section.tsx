import { useState } from "react";

// export function Section({
//   displayCompoent,
//   contentBase,
// }: {
//   displayComponent: React.FunctionComponent;
//   contentBase: object;
// }) {
//   const [contents, setContents] = useState(contentBase);
//   const [showForm, setShowForm] = useState(false);
//   return (showForm) ? <Form name="basic" structure={Object.values(contents)} /> : displayCompoent;
// }
